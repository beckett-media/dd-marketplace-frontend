import actionTypes from './actionTypes';

const initState = {
    marketPlace: [],
};

export default function reducer(state = initState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
